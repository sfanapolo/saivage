[Unit]
Description=Saivage AI Agent Server
After=network.target

[Service]
Type=simple
User=salva
Group=salva
WorkingDirectory=/opt/saivage
ExecStart=/usr/bin/node dist/cli.js serve /work/target
Restart=on-failure
RestartSec=5
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
